# Pinned to the versions that run green both locally and in CI.
#
# These were previously unpinned (>=), and the parquet stack
# (pandas/numpy/fastparquet) wasn't even listed — it came in transitively
# and floated. That let a pandas/numpy 2.x release silently break the
# nightly refresh: the workflow ran clean for weeks on unchanged code,
# then started failing ~20s into ingest.py the moment CI's fresh
# `pip install` pulled a new major. Local kept working because its venv
# still had the older versions. Pinning makes CI reproduce local exactly.
#
# To upgrade: bump deliberately, run scripts/ingest.py locally to confirm,
# and watch the next refresh run stay green before trusting it.
nfl_data_py==0.3.3
supabase==2.28.3
python-dotenv==1.2.2
pandas==1.5.3
numpy==1.26.4
fastparquet==2026.3.0
requests==2.33.1
